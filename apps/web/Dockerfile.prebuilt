FROM nginx:1.25-alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
# Expect prebuilt Flutter web at apps/web/build/web relative to build context
COPY build/web /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
